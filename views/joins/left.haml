%h1 LEFT JOIN

%p
  %pre
    %code
      T1 LEFT [OUTER] JOIN T2 ON boolean_expression
      T1 LEFT [OUTER] JOIN T2 USING ( join column list )
      T1 NATURAL LEFT [OUTER] JOIN T2

%p
  %img{src: '/images/img_leftjoin.gif'}


%h2 Ex2.1. simple
%hr

.row
  .col-md-6
    %h3 Books
    %table.table
      %thead
        %tr
          %th ID
          %th title
          %th category ID
      %tbody
        - @books.each do |book|
          %tr
            %td= book.id
            %td= book.title
            %td= book.category_id || "NULL"

  .col-md-6
    %h3 Categories
    %table.table
      %thead
        %tr
          %th ID
          %th name
      %tbody
        - @categories.each do |category|
          %tr
            %td= category.id
            %td= category.name


.row
  .col-md-12
    %h3 Result
    %table.table
      %thead
        %tr
          %th ID
          %th name
          %th category
      %tbody
        - @result_1.each do |row|
          %tr
            %td= row.id
            %td= row.title
            %td= row.category.try!(:name) || "NULL"

%h2 Ex2.2. find all categories that are NOT assigned to book (start query from categories)
%hr

.row
  .col-md-6
    %h3 Categories
    %table.table
      %thead
        %tr
          %th ID
          %th name
      %tbody
        - @categories.each do |category|
          %tr
            %td= category.id
            %td= category.name

  .col-md-6
    %h3 Books
    %table.table
      %thead
        %tr
          %th ID
          %th title
          %th category ID
      %tbody
        - @books.each do |book|
          %tr
            %td= book.id
            %td= book.title
            %td= book.category_id || "NULL"


.row
  .col-md-12
    %h3 Result
    %table.table
      %thead
        %tr
          %th ID
          %th name
      %tbody
        - @result_2.each do |row|
          %tr
            %td= row.id
            %td= row.name
